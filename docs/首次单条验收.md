# 首次单条投递验收

v1.3.0 默认启用首次单条验收，避免新用户一开始就批量运行。

## 流程

1. 完成简历、职业画像、投递方向和 AI 配置。
2. 选择全自动投递并开始任务。
3. 系统只需要完成第一条真实成功投递。
4. 第一条成功后队列自动暂停。
5. 用户检查：
   - 当前聊天对象正确；
   - 右侧聊天区出现完整文字气泡；
   - 简历附件符合设置；
   - 消息页记录为成功；
   - 没有重复向同一 HR 发送。
6. 确认无误后再次开始，进入正常批量流程。

已有成功投递历史的用户升级到 v1.3.0 时会自动视为已经完成验收，不会重复阻断。
