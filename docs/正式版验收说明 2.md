# UI37 验收重点

- 固定五页结构保持不变。
- 职业画像生成后自动推荐岗位方向，默认启用匹配度最高的 3 个。
- 用户可勾选、改名、改关键词、调优先级、删除或新增自定义方向。
- 未保存岗位方向时禁止启动新任务，并自动引导到方向选择模块。
- 搜索任务只从已确认方向生成，不混入未选择的画像关键词。
- 当前运行中的任务不被方向修改覆盖，新方向仅应用到下一轮任务。
- UI36 投递链路、自动排序、失败重试和 OpenClaw 保持原样。
