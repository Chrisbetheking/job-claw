# JobClaw v1.7.0 正式版交付说明

## 投递效率

- 精准投递和安全海投两套独立策略
- 安全海投只拦截明确硬性冲突 重复岗位和高风险岗位
- 安全海投快速模式默认不逐岗位调用AI 使用简历事实生成基础招呼语并进行本地排序
- 可切换AI个性化海投 需要更个性化时再逐岗位调用模型
- 保守 标准 高效 自定义四档投递节奏
- 发送动作始终单线程 避免并发写入同一聊天页面
- 页面变慢 网络超时和服务异常时自动提高等待时间
- OpenClaw不可用后进入5分钟快速降级 不会让每个岗位重复等待连接超时
- 队列预热数量可配置 安全海投默认积累3到4个岗位后排序发送

## OpenClaw连接

- HTTP本地桥接和Chrome Native Messaging双通道
- HTTP服务没有启动时 Native Messaging可自动唤醒服务
- macOS安装脚本同时安装LaunchAgent和Native Messaging Host
- 连接诊断显示HTTP Native通道 扩展ID 日志路径和修复建议
- 企业查询Provider不可用时自动降级本地规则 不阻塞整个投递任务

## 每日汇报

- OpenClaw每分钟检查一次本机汇报计划
- 默认每天20:30生成求职日报
- 汇总采集 AI分析 成功沟通 失败 风险拦截 重复跳过 模拟通过 当前队列和运行状态
- 报告保存到 `~/.jobclaw/reports/YYYY-MM-DD.md`
- `latest.md`始终保存最新一份日报
- macOS可通过通知中心主动提示日报摘要
- 侧边栏可修改汇报时间 立即生成和刷新日报

## 其他能力

- 统一限速 随机抖动 自适应退避和安全熔断
- 第三方企业查询Provider与本地风险规则
- 企业结果缓存 跨任务岗位去重和同公司每日上限
- 模拟运行 更新提醒 平台Adapter和Provider适配层
- 首次单条投递验收 可信发送证据 失败恢复和任务持久化

## 验证结果

- 单元测试7项通过
- 集成测试3项通过
- 回归测试35项通过
- Manifest V3 CSP JavaScript语法和敏感信息扫描通过
- 源码与可安装扩展同步检查通过
- OpenClaw `/status` `/sync` `/report/generate` 实际运行验证通过
- Native Messaging自动唤醒桥接实际验证通过

## 使用边界

- 本地风险规则只提供风险提示 不冒充工商核验
- 正式工商核验需要配置合法授权的企业数据Provider
- 企业查询密钥只能放在本机环境变量中
- 安全海投放宽岗位筛选 但不会放松重复发送 身份确认 验证码 熔断和发送证据要求
- 没有使用真实BOSS账号执行大批量自动投递 发布前仍应完成单条真实验收
